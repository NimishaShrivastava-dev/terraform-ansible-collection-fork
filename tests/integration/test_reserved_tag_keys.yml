---
- name: Test reserved_tag_keys integration target locally
  hosts: localhost
  gather_facts: false
  tasks:
    - name: Prepare Terraform Cloud credentials
      ansible.builtin.include_tasks:
        file: targets/prepare_tfc/tasks/main.yml

    - name: Run reserved tag keys integration tests
      ansible.builtin.include_tasks:
        file: targets/reserved_tag_keys/tasks/main.yml
