---
- name: Test registry_provider_platform integration target locally
  hosts: testhost
  gather_facts: false
  tasks:
    - name: Prepare Terraform Cloud credentials
      ansible.builtin.include_tasks:
        file: targets/prepare_tfc/tasks/main.yml

    - name: Run registry provider platform integration tests
      ansible.builtin.include_tasks:
        file: targets/registry_provider_platform/tasks/main.yml
