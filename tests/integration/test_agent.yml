---
- name: Test agent integration target locally
  hosts: localhost
  gather_facts: false
  tasks:
    - name: Prepare Terraform Cloud credentials
      ansible.builtin.include_tasks:
        file: targets/prepare_tfc/tasks/main.yml

    - name: Run agent integration tests
      ansible.builtin.include_tasks:
        file: targets/agent/tasks/main.yml
